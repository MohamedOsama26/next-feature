import 'package:next_feature/cli.dart';

/// Command-line entry point for the Next Feature tool.
/// This CLI tool helps in generating feature modules for Flutter applications.
void main(List<String> arguments) {
  CLI().run(arguments);
}
